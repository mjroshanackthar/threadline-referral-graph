// Simple runtime config so the built frontend can point at any deployed
// backend without a rebuild — just edit this file (or template it in CI).
window.API_BASE = window.API_BASE || "http://localhost:4000";
